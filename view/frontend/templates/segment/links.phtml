<?php

$segments = $block->getActiveSegments();
foreach ($segments as $segment) {
    echo "<span class='item d-none d-xl-inline-block'>".
        "<a href='".$block->getSegmentUrl($segment)."'>{$segment['name']}</a>".
        "</span>";
}
echo "<span class='item d-none d-xl-inline-block'><a href='".$block->getContactLink()."'>Kontakt</a></span>";
