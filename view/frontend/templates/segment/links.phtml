<?php

$segments = $block->getActiveSegments();
foreach ($segments as $segment) {
    echo "<div class='col-auto d-none d-xl-inline-block'>".
        "<a href='".$block->getSegmentUrl($segment)."'>{$segment['name']}</a>".
        "</div>";
}
echo "<div class='col-auto d-none d-xl-inline-block'><a href='".$block->getContactLink()."'>Kontakt</a></div>";
